# Streamura Backend Requirements
# Python 3.11+ (developed/tested on 3.14)
# Versions pinned to the known-good set installed in the project venv.

# Core Framework
fastapi==0.136.3
uvicorn==0.48.0
python-dotenv==1.0.0
slowapi==0.1.9

# Database
psycopg2-binary==2.9.12
sqlalchemy==2.0.49
alembic==1.18.4

# Async Support
httpx==0.25.2
python-multipart==0.0.6

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2

# Validation / Settings
pydantic==2.13.4
pydantic-settings==2.14.1
email-validator==2.3.0

# Real-time
redis==5.0.1
websockets==12.0

# Event Detection
scikit-learn==1.8.0
numpy==2.4.6
geopy==2.4.1

# Content Moderation (Phase 9)
better-profanity==0.7.0

# Monetization
stripe==7.8.0

# Notifications
twilio==8.10.0

# Monitoring
prometheus-client==0.19.0
sentry-sdk==1.38.0

# Testing
pytest==7.4.3
pytest-asyncio==0.23.2

# Development
black==23.12.1
flake8==6.1.0
isort==5.13.2
mypy==1.7.1

# Seed Data
faker==22.0.0

# --- Optional integrations (install only when enabling the real provider) ---
# These are imported lazily/guarded; the app runs in demo/mock mode without them.
# boto3                 # AWS S3 storage + Rekognition video moderation
# resend                # transactional email
# openai                # model_router / video moderation (vision)
# anthropic             # model_router
# livekit               # real LiveKit server token minting (STREAMURA_DEMO_MODE=false)
# livekit-api
# pywebpush             # VAPID web push notifications
